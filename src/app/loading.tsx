import { Loader2 } from "lucide-react";

export default function Loading() {
  return (
    <Loader2 className="mx-auto my-7 min-h-screen w-[100vw] animate-spin bg-accent text-muted-foreground" />
  );
}
